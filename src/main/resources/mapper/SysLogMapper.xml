<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.springbootcargovision.mapper.SysLogMapper">

    <resultMap id="BaseResultMap" type="com.app.springbootcargovision.model.SysLog">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="module" property="module" jdbcType="VARCHAR"/>
        <result column="type" property="type" jdbcType="VARCHAR"/>
        <result column="description" property="description" jdbcType="VARCHAR"/>
        <result column="operator" property="operator" jdbcType="VARCHAR"/>
        <result column="ip" property="ip" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="error_msg" property="errorMsg" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, module, type, description, operator, ip, status, error_msg, create_time
    </sql>

    <select id="selectLogList" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sys_log
        <where>
            <if test="log.module != null and log.module != ''">
                and module like concat('%', #{log.module}, '%')
            </if>
            <if test="log.operator != null and log.operator != ''">
                and operator like concat('%', #{log.operator}, '%')
            </if>
            <if test="log.status != null and log.status != ''">
                and status = #{log.status}
            </if>
            <if test="startTime != null and startTime != ''">
                and create_time &gt;= #{startTime}
            </if>
            <if test="endTime != null and endTime != ''">
                and create_time &lt;= #{endTime}
            </if>
        </where>
        order by create_time desc
    </select>

    <insert id="insertLog" parameterType="com.app.springbootcargovision.model.SysLog" useGeneratedKeys="true" keyProperty="id">
        insert into sys_log (
            module, type, description, operator, ip, status, error_msg, create_time
        ) values (
            #{module}, #{type}, #{description}, #{operator}, #{ip}, #{status}, #{errorMsg}, #{createTime}
        )
    </insert>

    <delete id="deleteLogByIds">
        delete from sys_log where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <delete id="cleanLog">
        truncate table sys_log
    </delete>

</mapper>
